# Task 08 — Passkey Private Archive

## Purpose

기존 공개 소개 페이지는 그대로 유지하고, 패스키 인증 이후에만 접근할 수 있는 비공개 공간을 추가한다.

이 기능에는 비밀번호를 사용하지 않는다.

실제 개인정보는 저장하거나 화면에 표시하지 않는다. 비공개 자료는 과제용 가상 데이터만 사용한다.

## Non-negotiable rules

1. 모든 신규 DB 테이블 이름은 `t08_`로 시작한다.
2. 기존 공개 소개 페이지는 로그인 없이 계속 열려야 한다.
3. 비로그인 응답의 HTML, RSC payload, JSON, script, source map 등에 비공개 자료가 포함되면 안 된다.
4. 비공개 자료를 클라이언트에 미리 전달한 뒤 CSS로 숨기지 않는다.
5. 비공개 자료는 인증된 서버 요청 이후에만 내려준다.
6. 비밀번호 입력창과 비밀번호 인증 로직을 만들지 않는다.
7. WebAuthn 검증 결과가 성공했을 때만 로그인 세션을 생성한다.
8. 개인키를 서버로 전송하거나 DB에 저장하지 않는다.
9. 클라이언트가 보낸 `userId`, `ownerId`, `username`을 자료 소유권 판단에 사용하지 않는다.
10. 자료 소유자는 항상 서버 세션의 `userId`로 결정한다.
11. challenge는 매 요청마다 새로 생성하고, 만료 시간과 1회 사용 제한을 둔다.
12. 이미 사용됐거나 만료된 challenge는 반드시 거절한다.
13. 로그아웃하면 기존 세션은 다시 사용할 수 없어야 한다.
14. 패스키 삭제 시 현재 로그인 사용자의 소유권을 확인한다.
15. 마지막 남은 패스키는 삭제하지 못하게 막는다.
16. 실제 토큰, 쿠키, 세션값, 환경변수, 초대 토큰을 로그나 문서에 남기지 않는다.
17. 기존 기능과 디자인을 필요 이상으로 변경하지 않는다.
18. 테스트하지 않은 결과를 성공했다고 문서에 쓰지 않는다.

## Required database tables

* `t08_users`
* `t08_passkeys`
* `t08_auth_challenges`
* `t08_sessions`
* `t08_private_items`

추가 테이블이 필요하다면 반드시 `t08_` 접두사를 사용하고, 왜 필요한지 완료 보고에 적는다.

## Authentication model

패스키 구현은 현재 프로젝트와 호환되는 다음 라이브러리를 우선 사용한다.

* `@simplewebauthn/server`
* `@simplewebauthn/browser`

설치된 버전의 타입과 공식 API를 기준으로 구현한다.

등록 흐름:

1. 서버가 registration challenge를 생성한다.
2. 서버가 challenge를 짧은 시간 동안 보관한다.
3. 브라우저가 패스키 생성을 요청한다.
4. 서버가 registration response를 검증한다.
5. 검증 성공 후 공개키만 `t08_passkeys`에 저장한다.
6. 검증 실패 또는 사용자 취소 시 아무것도 저장하지 않는다.

로그인 흐름:

1. 서버가 authentication challenge를 새로 생성한다.
2. 브라우저가 패스키로 challenge에 서명한다.
3. 서버가 저장된 공개키로 서명을 검증한다
